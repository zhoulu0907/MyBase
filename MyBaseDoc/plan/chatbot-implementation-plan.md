# Chatbot 工作台实施计划

**版本**：v2.0.0
**日期**：2026-04-25
**状态**：待开始
**参考文档**：`../PRD/chatbot-prd.md`

**版本历史**：
- v1.0.0 (2026-04-25)：初始版本
- v2.0.0 (2026-04-25)：补充 V2.0 需求 - LLM 配置功能

---

## 概览

| 阶段 | 名称 | 预估工期 | 测试方式 |
|-----|------|---------|---------|
| 阶段一 | 工程搭建 | 2 天 | 构建验证 |
| 阶段二 | 核心功能开发 | 4 天 | 单元测试 + 组件测试 |
| 阶段三 | 安全与配置功能 | 2 天 | 集成测试 |
| 阶段四 | 前后端联调 | 2 天 | 端到端测试 |
| 阶段五 | 验收与上线 | 2 天 | 验收测试 |
| **阶段六** | **V2.0 LLM 配置** | **2 天** | **配置功能测试** |

**总工期**：14 个工作日（含 V2.0）

---

## 阶段��：工程搭建

### 1.1 阶段目标

搭建前后端工程结构，验证开发环境可用性。

### 1.2 前端任务

| 任务 | 说明 | 产出物 |
|-----|------|-------|
| T-F01 | 搭建 Chatbot 组件目录结构 | `packages/ui-kit/src/components/Workbench/Chatbot/` |
| T-F09 | 配置 ESLint + Prettier + TypeScript | 配置文件更新 |

**前端验收标准**：
- `pnpm build:packages` 构建成功
- `pnpm tsc --noEmit` 无类型错误
- `pnpm eslint` 无 error

### 1.3 后端任务

| 任务 | 说明 | 产出物 |
|-----|------|-------|
| T-B01 | 搭建 onebase-module-ai 多模块 Maven 结构 | `onebase-module-ai/` 目录 |
| T-B08 | 配置 Swagger/OpenAPI 文档 | Knife4j 集成 |

**后端验收标准**：
- `mvn compile` 编译成功
- 访问 `http://localhost:48080/doc.html` 可看到 API 文档
- 项目结构符合多模块规范

### 1.4 测试用例

```
【前端-构建验证】
用例编号: F-UT-001
用例名称: 前端包构建测试
测试步骤:
  1. 执行 pnpm build:packages
  2. 检查 dist 输出目录
预期结果:
  - 构建成功，无 error
  - 生成 ui-kit 的产物

【前端-类型检查】
用例编号: F-UT-002
用例名称: TypeScript 类型检查
测试步骤:
  1. 执行 pnpm tsc --noEmit
预期结果:
  - 无类型错误

【后端-编译验证】
用例编号: B-UT-001
用例名称: 后端项目编译测试
测试步骤:
  1. 执行 mvn compile
预期结果:
  - 编译成功
  - 生成 onebase-module-ai 的 class 文件

【后端-API文档】
用例编号: B-UT-002
用例名称: Swagger文档可访问
测试步骤:
  1. 启动后端应用
  2. 访问 http://localhost:48080/doc.html
预期结果:
  - 页面正常加载
  - 可看到 AI 模块相关接口占位
```

### 1.5 成功标准

- [ ] 前端构建通过
- [ ] 后端编译通过
- [ ] API 文档可访问
- [ ] 无阻塞性环境问题

---

## 阶段二：核心功能开发

### 2.1 阶段目标

完成前端组件和后端接口的核心功能开发。

### 2.2 前端任务

| 任务 | 说明 | 产出物 |
|-----|------|-------|
| T-F02 | 实现 ChatbotWorkbench 主组件 | `ChatbotWorkbench.tsx` |
| T-F03 | 实现 ChatInput 输入框组件 | `ChatInput.tsx` |
| T-F04 | 实现 ChatMessage 消息渲染组件 | `ChatMessage.tsx` |
| T-F05 | 实现 ProviderSelector 选择器组件 | `ProviderSelector.tsx` |
| T-F07 | 实现 useStream 流式读取 hook | `useStream.ts` |
| T-F06 | 实现 useChat 聊天逻辑 hook | `useChat.ts` |
| T-F08 | 集成 Markdown 渲染与代码高亮 | Markdown 组件集成 |

**前端验收标准**：
- 组件能正常渲染
- 消息输入功能可用
- Markdown 能正确渲染
- SSE 流式数据能接收

### 2.3 后端任务

| 任务 | 说明 | 产出物 |
|-----|------|-------|
| T-B02 | 实现 ChatController 流式接口 | `POST /ai/chat` 接口 |
| T-B03 | 实现 LLM Provider 代理服务 | 第三方 LLM 调用封装 |
| T-B07 | ���现 GET /chat/providers 接口 | `GET /ai/chat/providers` 接口 |

**后端验收标准**：
- `/ai/chat` 接口返回 SSE 流式响应
- `/ai/chat/providers` 返回可用 Provider 列表
- 正确转发请求到第三方 LLM

### 2.4 测试用例

```
【前端-组件渲染】
用例编号: F-UT-003
用例名称: Chatbot组件渲染测试
测试步骤:
  1. 在测试页面引入 ChatbotWorkbench 组件
  2. 传入模拟的 providers 配置
预期结果:
  - 组件正常挂载
  - 无 console error

【前端-消息输入】
用例编号: F-UT-004
用例名称: 消息输入功能测试
前置条件: 组件已正确渲染
测试步骤:
  1. 在输入框输入文字
  2. 点击发送按钮或按 Enter
预期结果:
  - 输入框内容清空（如果配置了mock）
  - 触发 onSend 回调

【前端-Markdown渲染】
用例编号: F-UT-005
用例名称: Markdown渲染测试
测试步骤:
  1. 渲染包含 Markdown 的消息内容
  2. 验证代码块、列表、链接等格式
预期结果:
  - Markdown 正确渲染
  - 代码块有高亮效果

【前端-SSE解析】
用例编号: F-UT-006
用例名称: SSE流式数据解析测试
前置条件: Mock SSE 响应
测试步骤:
  1. 模拟 SSE 响应: data: {"delta": "hello"}
  2. 验证 delta 内容被正确解析
预期结果:
  - delta 内容被正确提取
  - done=true 时流式结束

【后端-流式接口】
用例编号: B-UT-003
用例名称: POST /ai/chat 流式响应测试
前置条件: 后端服务运行中
测试步骤:
  1. 发送 POST 请求到 /admin-api/ai/chat
  2. 使用 curl -N 保持连接
预期结果:
  - 返回 Content-Type: text/event-stream
  - 能接收到流式数据

【后端-Provider列表】
用例编号: B-UT-004
用例名称: GET /ai/chat/providers 接口测试
测试步骤:
  1. 发送 GET 请求到 /admin-api/ai/chat/providers
  2. 携带正确的 X-Tenant-Id 头
预期结果:
  - 返回 200
  - 返回当前租户的 Provider 列表
```

### 2.5 成功标准

- [ ] 前端所有组件完成并能正常渲染
- [ ] 后端 Chat 接口支持 SSE 流式输出
- [ ] 前端能正确解析 SSE 数据
- [ ] Markdown 渲染和代码高亮正常
- [ ] 单元测试覆盖率达标

---

## 阶段三：安全与配置功能

### 3.1 阶段目标

完成租户隔离、SM2 加密、频控等安全功能。

### 3.2 后端任务

| 任务 | 说明 | 产出物 |
|-----|------|-------|
| T-B04 | 实现租户隔离拦截器 | TenantContextHolder + 拦截器 |
| T-B05 | 实现 SM2 加密解密工具 | SM2Util 工具类 |
| T-B06 | 实现 Redis 频控组件 | RateLimiter 组件 |

**后端验收标准**：
- 跨租户请求被正确拦截
- SM2 加解密功能正常
- 频控超出限制返回 429

### 3.3 测试用例

```
【后端-租户隔离】
用例编号: B-IT-001
用例名称: 租户隔离验证
前置条件: 已实现租户拦截器
测试步骤:
  1. 发送请求 A，X-Tenant-Id: 1
  2. 发送请求 B，X-Tenant-Id: 2
  3. 验证两个请求的数据隔离
预期结果:
  - 租户 1 无法访问租户 2 的数据
  - 无跨租户数据泄露

【后端-租户缺失】
用例编号: B-IT-002
用例名称: 租户ID缺失拦截
测试步骤:
  1. 发送请求时不携带 X-Tenant-Id
预期结果:
  - 返回 401 或 403
  - 请求被拒绝

【后端-SM2解密】
用例编号: B-IT-003
用例名称: SM2加密解密测试
前置条件: 已实现 SM2Util
测试步骤:
  1. 使用公钥加密字符串
  2. 使用私钥解密
  3. 验证解密结果与原字符串一致
预期结果:
  - 加密解密成功
  - 结果一致

【后端-频控触发】
用例编号: B-IT-004
用例名称: 频率限制测试
前置条件: 频控组件已集成
测试步骤:
  1. 在短时间内发送超过限制数量的请求
  2. 触发频控限制
预期结果:
  - 返回 HTTP 429
  - 包含 Retry-After 头
  - 响应体包含限流提示

【后端-频控多维度】
用例编号: B-IT-005
用例名称: 多维度频控测试
测试步骤:
  1. 验证租户级别频控
  2. 验证用户级别频控
  3. 验证 IP 级别频控
预期结果:
  - 各维度独立生效
  - 不相互干扰
```

### 3.4 成功标准

- [ ] 租户隔离功能正常，跨租户请求被拒绝
- [ ] SM2 加解密功能正常
- [ ] 频控功能正常，超限返回 429
- [ ] 所有安全相关测试通过

---

## 阶段四：前后端联调

### 4.1 阶段目标

前后端对接，完成端到端功能验证。

### 4.2 联调任务

| 任务 | 说明 | 产出物 |
|-----|------|-------|
| T-I01 | 前后端接口联调 | 完整对话流程 |
| T-I02 | 流式输出专项测试 | SSE 稳定性验证 |
| T-I03 | 租户隔离验证 | 多租户场景验证 |
| T-I04 | 频控功能验证 | 前后端频控联动 |
| T-I05 | 异常场景联调 | 错误处理验证 |

**联调验收标准**：
- 前端发送消息，后端流式返回，前端正确展示
- 多 Provider 切换正常工作
- 错误场景有友好提示

### 4.3 测试用例

```
【联调-完整对话】
用例编号: E2E-001
用例名称: 完整对话流程测试
前置条件: 前后端服务正常运行，配置了真实或Mock LLM
测试步骤:
  1. 启动前端应用
  2. 进入 Chatbot 工作台
  3. 输入 "你好" 并发送
  4. 等待 AI 回复
预期结果:
  - 消息发送成功
  - 收到流式响应
  - 消息内容正确显示在界面上

【联调-流式稳定性】
用例编号: E2E-002
用例名称: 流式输出稳定性测试
前置条件: 已配置真实 LLM Provider
测试步骤:
  1. 发送一个需要较长回复的问题
  2. 监控流式输出��程
预期结果:
  - 流式输出稳定不断开
  - 内容完整无截断
  - 首字延迟 < 1s

【联调-Provider切换】
用例编号: E2E-003
用例名称: 多Provider切换测试
前置条件: 已配置多个 Provider
测试步骤:
  1. 使用 OpenAI Provider 发送消息
  2. 切换到 DeepSeek Provider
  3. 发送相同消息
  4. 对比两次回复
预期结果:
  - 都能正常返回回复
  - 使用对应 Provider 的模型

【联调-租户隔离】
用例编号: E2E-004
用例名称: 多租户隔离联调
前置条件: 至少两个租户账户
测试步骤:
  1. 租户 A 发送消息，验证返回数据属于租户 A
  2. 租户 B 发送消息，验证返回数据属于租户 B
预期结果:
  - 租户 A 无法看到租户 B 的数据
  - 数据严格隔离

【联调-频控触发】
用例编号: E2E-005
用例名称: 频控触发联调测试
前置条件: 频控组件已配置
测试步骤:
  1. 在前端快速发送超过限制数量的消息
  2. 观察前端显示
预期结果:
  - 超出频控后返回友好错误提示
  - 不出现技术性错误信息

【联调-错误恢复】
用例编号: E2E-006
用例名称: 异常场景错误处理
测试步骤:
  1. 模拟 LLM 服务不可用场景
  2. 发送消息
预期结果:
  - 显示友好错误提示
  - 不出现 500 等技术性错误
  - 应用不崩溃
```

### 4.4 成功标准

- [ ] 完整对话流程可正常工作
- [ ] 流式输出稳定
- [ ] 多 Provider 切换正常
- [ ] 租户隔离在端到端场景下生效
- [ ] 错误处理友好

---

## 阶段五：验收与上线

### 5.1 阶段目标

完成最终验收，准备上线。

### 5.2 任务

| 任务 | 说明 | 产出物 |
|-----|------|-------|
| T-I06 | 安全扫描与修复 | 安全报告 |
| - | 编写验收测试报告 | 验收文档 |
| - | 代码审查与优化 | CR 报告 |
| - | 上线部署 | 部署包 |

### 5.3 验收测试用例

```
【验收-前端全功能】
用例编号: UAT-F-001
用例名称: 前端功能验收
测试步骤:
  1. 组件渲染正常
  2. 消息输入/发送正常
  3. Markdown 渲染正确
  4. 流式输出正常
  5. Provider 切换正常
  6. 加载状态显示正常
  7. 消息清空正常
预期结果:
  - 所有功能正常
  - 无 console error

【验收-后端全功能】
用例编号: UAT-B-001
用例名称: 后端功能验收
测试步骤:
  1. /ai/chat 接口流式响应正常
  2. /ai/chat/providers 接口正常
  3. 租户隔离生效
  4. SM2 加解密正常
  5. 频控生效
  6. 错误处理正常
预期结果:
  - 所有接口符合规范
  - 安全功能正常

【验收-性能指标】
用例编号: UAT-P-001
用例名称: 性能指标验证
测试步骤:
  1. 测量首字延迟
  2. 测试并发连接数
  3. 测试吞吐量
预期结果:
  - 首字延迟 < 1s
  - 并发数 >= 100
  - 吞吐量 >= 50 QPS

【验收-安全扫描】
用例编号: UAT-S-001
用例名称: 安全扫描
测试步骤:
  1. 使用安全扫描工具扫描
  2. 检查敏感信息泄露
  3. 检查 XSS 等漏洞
预期结果:
  - 无高危漏洞
  - 敏感信息已脱敏
  - 符合安全规范

【验收-文档完整性】
用例编号: UAT-D-001
用例名称: 文档验收
检查项:
  1. API 文档完整
  2. 组件使用文档完整
  3. 部署文档完整
预期结果:
  - 文档齐全
  - 可按文档复现功能
```

### 5.4 成功标准

- [ ] 所有验收测试通过
- [ ] 安全扫描无高危漏洞
- [ ] 性能指标达标
- [ ] 文档完整
- [ ] 代码审查通过

---

## 阶段六：V2.0 LLM 配置功能

### 6.1 阶段目标

在 app-builder 的右侧属性面板中增加"LLM 配置"分组，实现配置��的可见性和双向绑定。

### 6.2 前端任务

| 任务 | 说明 | 产出物 |
|-----|------|-------|
| V2-F01 | 修改 Material Schema 增加 LLM 配置分组 | `ChatbotAgent/schema.ts` 更新 |
| V2-F02 | 实现 Provider 下拉选择器 | Select 组件配置 |
| V2-F03 | 实现 API Key 密码输入框 | Input.Password 组件 |
| V2-F04 | 实现配置双向绑定 | onChange 事件处理 |
| V2-F05 | 实现配置持久化 | 状态管理集成 |
| V2-F06 | 实现空状态提示 | apiKey 为空时的 UI 提示 |

**前端验收标准**：
- 属性面板显示"LLM 配置"折叠栏
- 配置项完整显示（provider, model, apiKey, baseUrl, systemPrompt）
- 配置修改实时生效
- 切换组件后配置保持

### 6.3 后端任务

| 任务 | 说明 | 产出物 |
|-----|------|-------|
| V2-B01 | 确保 API 接口支持 baseUrl 参数 | `ChatProxyRequest.java` 更新 |
| V2-B02 | 支持自定义 baseUrl 转发 | `ChatProxyServiceImpl.java` 更新 |

**后端验收标准**：
- 接口能接收并使用自定义 baseUrl
- 支持私有模型调用

### 6.4 测试用例

```
【V2-UT-001: LLM配置分组显示测试】
用例编号: V2-UT-001
用例名称: LLM配置分组显示测试
前置条件: app-builder 服务已启动，ChatbotAgent 组件已注册
测试步骤:
  1. 启动 app-builder 应用
  2. 从左侧组件面板的"工作台组件"分类中拖入"AI 对话助手"组件到画布
  3. 选中画布中的 AI 助手组件
  4. 观察右侧属性面板
预期结果:
  - 属性面板中显示"LLM 配置"折叠栏
  - 折叠栏默认处于收起状态
  - 折叠栏有展开/收起箭头指示

【V2-UT-002: LLM配置项完整性测试】
用例编号: V2-UT-002
用例名称: LLM配置项完整性测试
前置条件: 已选中 AI 助手组件
测试步骤:
  1. 点击展开"LLM 配置"折叠栏
  2. 逐一检查以下配置项是否存在:
     - AI提供商 (provider) - 下拉选择框
     - API地址 (baseUrl) - 输入框
     - 模型名称 (model) - 输入框
     - API Key (apiKey) - 密码输入框
     - 系统提示词 (systemPrompt) - 多行文本框
  3. 检查默认值是否正确填充
预期结果:
  - 所有 5 个配置项完整显示，无缺失
  - provider 默认值为 "OpenAI"
  - baseUrl 根据 provider 自动填充对应地址
  - model 有默认模型名
  - systemPrompt 有默认提示词

【V2-UT-003: Provider下拉选择测试】
用例编号: V2-UT-003
用例名称: Provider下拉选择测试
前置条件: 已展开"LLM 配置"折叠栏
测试步骤:
  1. 点击"AI提供商"下拉框
  2. 确认下拉选项包含: OpenAI, DeepSeek, Anthropic, Custom
  3. 选择 "OpenAI"，观察 baseUrl 自动填充值
  4. 选择 "DeepSeek"，观察 baseUrl 自动填充值
  5. 选择 "Anthropic"，观察 baseUrl 自动填充值
  6. 选择 "Custom"，观察 baseUrl 变为可编辑状态
预期结果:
  - 下拉列表包含至少 4 个选项: OpenAI, DeepSeek, Anthropic, Custom
  - 选择预设 Provider 时，baseUrl 自动填充对应地址（不可编辑）
  - 选择 Custom 时，baseUrl 输入框变为可编辑状态
  - Provider 切换后，其他配置项可能需要重新填写

【V2-UT-004: 配置双向绑定测试】
用例编号: V2-UT-004
用例名称: 配置双向绑定测试
前置条件: 已展开"LLM 配置"折叠栏
测试步骤:
  1. 在 API Key 输入框中输入测试密钥: test-api-key-12345
  2. 切换到画布上的其他组件（点击其他组件）
  3. 等待 2 秒
  4. 再次点击选中 AI 助手组件
  5. 回到"LLM 配置"折叠栏，查看 API Key 输入框
预期结果:
  - 之前输入的 API Key 内容 "test-api-key-12345" 仍然存在
  - 配置值在组件切换后保持不变
  - 证明配置双向绑定功能正常

【V2-UT-005: API Key空状态提示测试】
用例编号: V2-UT-005
用例名称: API Key空状态提示测试
前置条件: 已选中 AI 助手组件，apiKey 未配置
测试步骤:
  1. 确保 API Key 输入框为空（不填写）
  2. 观察画布中 AI 助手组件的预览区域
  3. 尝试在聊天输入框输入内容
预期结果:
  - 画布预览区域显示"请先配置 API Key"提示
  - 聊天输入框可能显示为禁用状态或显示提示
  - 不出现技术性错误信息
  - 提示信息友好、易理解

【V2-UT-006: 自定义BaseUrl转发测试】
用例编号: V2-UT-006
用例名称: 自定义BaseUrl转发测试
前置条件: 后端服务运行中，已配置 Custom Provider
测试步骤:
  1. 在"LLM 配置"面板中配置:
     - Provider: Custom（自定义）
     - BaseUrl: http://localhost:11434/v1
     - Model: llama3
     - ApiKey: test-key
  2. 在聊天输入框输入测试消息: "你好"
  3. 点击发送按钮
  4. 使用抓包工具或日志查看实际请求地址
预期结果:
  - 请求发送到 http://localhost:11434/v1/chat/completions
  - 请求体包含正确的 model: "llama3"
  - 请求头包含 Authorization: Bearer test-key
  - 如果本地 Ollama 可用，应能获取到响应

【V2-UT-007: 配置字段名对应测试】
用例编号: V2-UT-007
用例名称: 配置字段名对应测试
前置条件: 前端和后端代码已实现
测试步骤:
  1. 检查前端 schema 文件中 LLM 配置相关字段定义
     - 查看 ChatbotAgent/schema.ts 或类似文件
  2. 检查后端 ChatProxyRequest.java 中的字段定义
     - 查看 onebase-module-ai 中的 ChatProxyRequest.java
  3. 对比以下字段名是否完全对应:
     - provider
     - baseUrl
     - model
     - apiKey
     - systemPrompt
预期结果:
  - 前端 schema 字段名与后端 ChatProxyRequest 字段名完全一致
  - 无大小写差异（如 baseUrl vs baseURL）
  - 无命名风格差异（如 apiKey vs api_key）
  - 字段类型相互兼容
```
【V2-配置可见性】
用例编号: V2-UT-001
用例名称: LLM配置分组显示测试
前置条件: 组件已注册到 ui-kit
测试步骤:
  1. 启动 app-builder
  2. 从工作台组件分类中拖入"AI 对话助手"
  3. 选中画布中的 AI 助手组件
  4. 查看右侧属性面板
预期结果:
  - 出现"LLM 配置"折叠栏
  - 展开后显示所有配置项

【V2-配置项完整】
用例编号: V2-UT-002
用例名称: LLM配置项完整性测试
测试步骤:
  1. 展开"LLM 配置"折叠栏
  2. 检查以下配置项是否存在:
     - AI提供商 (provider)
     - API地址 (baseUrl)
     - 模型名称 (model)
     - API Key (apiKey)
     - 系统提示词 (systemPrompt)
预期结果:
  - 所有配置项完整显示
  - 默认值正确填充

【V2-Provider选择】
用例编号: V2-UT-003
用例名称: Provider下拉选择测试
测试步骤:
  1. 展开"LLM 配置"
  2. 点击"AI提供商"下拉框
  3. 选择不同的 Provider
预期结果:
  - 下拉选项包含: OpenAI, DeepSeek, Anthropic, Custom
  - 选择后 baseUrl 自动填充对应地址

【V2-配置双向绑定】
用例编号: V2-UT-004
用例名称: 配置双向绑定测试
测试步骤:
  1. 在 API Key 输入框输入测试密钥
  2. 切换到其他组件
  3. 再切换回 AI 助手组件
预期结果:
  - 输入的 API Key 内容保持存在
  - 配置已持久化

【V2-空状态提示】
用例编号: V2-UT-005
用例名称: API Key��状态提示测试
前置条件: apiKey 未配置
测试步骤:
  1. 选中 AI 助手组件
  2. 不填写 API Key
  3. 观察组件预览区域
预期结果:
  - 显示"请先配置 API Key"提示
  - 不显示聊天输入框

【V2-后端自定义URL】
用例编号: V2-UT-006
用例名称: 自定义BaseUrl转发测试
前置条件: 后端服务运行中
测试步骤:
  1. 在配置面板填写:
     - Provider: Custom
     - BaseUrl: http://localhost:11434/v1
     - Model: llama3
     - ApiKey: test-key
  2. 发送测试消息
预期结果:
  - 请求发送到自定义 BaseUrl
  - 能够获取到响应（如果本地模型可用）

【V2-代码合规】
用例编号: V2-UT-007
用例名称: 配置字段名对应测试
测试步骤:
  1. 检查前端 schema 中的字段名
  2. 检查后端 ChatProxyRequest 中的字段名
  3. 对比字段名是否一致
预期结果:
  - provider, model, apiKey, baseUrl 字段名完全对应
```

### 6.5 成功标准

- [ ] "LLM 配置"折叠栏在属性面板中显示
- [ ] 所有配置项（provider, model, apiKey, baseUrl, systemPrompt）完整
- [ ] 配置修改实时生效
- [ ] 切换组件后配置保持
- [ ] apiKey 为空时显示友好提示
- [ ] 后端支持自定义 baseUrl

---

## 里程碑

| 里程碑 | 日期 | 验收方式 |
|-------|-----|---------|
| M1：工程就绪 | +2d | 阶段一测试全部通过 |
| M2：核心功能完成 | +6d | 阶段二测试全部通过 |
| M3：安全功能完成 | +8d | 阶段三测试全部通过 |
| M4：联调完成 | +10d | 阶段四测试全部通过 |
| M5：上线验收 | +12d | 阶段五测试全部通过 |
| M6：LLM配置完成 | +14d | 阶段六测试全部通过 |

---

## 测试执行要求

### 测试环境要求

| 环境 | 要求 |
|-----|------|
| 后端 | Spring Boot 3.4.5 + JDK 17，端口 48080 |
| 前端 | Node 18+，pnpm 8+ |
| 数据库 | PostgreSQL（测试实例） |
| Redis | 用于频控测试 |
| 第三方 | 至少一个可用的 LLM API（可Mock） |

### 测试通过准则

- 每个测试用例必须记录执行结果
- 用例失败必须记录原因和修复方案
- 回归测试必须覆盖已修复的问题
- 阶段切换前必须所有用例通过

### 测试报告要求

每个阶段结束时必须输出：
- 测试用例执行报告
- 测试覆盖率报告
- 问题清单和修复状态
- 是否满足阶段成功标准

---

**计划结束**