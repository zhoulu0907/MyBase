export enum ETLNodeType {
  INPUT_NODE = 'input',
  OUTPUT_NODE = 'output',
  JOIN_NODE = 'join',
  UNION_NODE = 'union'
}

export enum ETLDrawerTab {
  DATA_PREVIEW = 'dataPreview',
  NODE_REMARK = 'nodeRemark'
}
