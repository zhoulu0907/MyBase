import { memo, useEffect, useState } from 'react';
import {
  Button,
  DatePicker,
  Form,
  Grid,
  Input,
  InputNumber,
  Select,
  Switch,
  TreeSelect,
  type FormInstance
} from '@arco-design/web-react';
import type { TableSearchConfig } from './schema';


const TableSearch  = memo((props:TableSearchConfig)=>{
    const { searchItems, form } = props;
    return <></>
})

export default TableSearch;