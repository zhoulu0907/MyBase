import { memo } from 'react';
import '../index.css';

const XPlaceholder = memo(() => {

  return (
    <div
      className='formWrapper'
      style={{
        height: 30
      }}
    />
  );
});

export default XPlaceholder;
