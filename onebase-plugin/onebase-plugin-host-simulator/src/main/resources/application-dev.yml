# 插件配置（开发模式 - 直接扫描 classpath）
# 此模式下直接扫描 classpath 中的扩展点，无需打包 ZIP/JAR
onebase:
  plugin:
    enabled: true
    auto-load: true
    auto-start: true
    mode: dev              # 开发模式，只扫描 classpath，不扫描plugins-dir
    plugins-dir: ./plugins # 插件目录（开发模式下不扫描该目录）

