# 插件配置（预发布模式 - 全生命周期验证）
# 此模式只加载plugins目录的ZIP/JAR插件，不扫描classpath，用于验证插件完整生命周期
onebase:
  plugin:
    enabled: true
    mode: staging             # 预发布模式，只加载 plugins-dir 的ZIP包，不扫描classpath
    plugins-dir: ./plugins    # 插件目录，存放 ZIP/JAR 插件包

